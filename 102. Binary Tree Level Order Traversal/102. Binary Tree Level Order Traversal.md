# 問題

- 二分木が与えられる
- 同じ深さごとに値を格納したリストを返す
- 空リストも与えられうる

# Step1

- 同じ深さをまとめて返すことからBFS

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def levelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:
        level_order_val = []
        
        if not root:
            return level_order_val

        same_level = deque([root])

        while same_level:
            level_size = len(same_level)
            same_level_val = []
            for _ in range(level_size):
                node = same_level.popleft()
                same_level_val.append(node.val)
                if node.left:
                    same_level.append(node.left)
                if node.right:
                    same_level.append(node.right)
            level_order_val.append(same_level_val)

        return level_order_val
```

# Step2

### 他の人のコード

1. DFS
- https://github.com/rimokem/arai60/pull/26
- https://github.com/h-masder/Arai60/pull/29

再帰でもぐる場合

- トップダウンでかく
    - 伝える情報は深さと命令

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def levelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:
        node_groups = []

        def tree_traverse(node, depth):
            if not node:
                return

            if depth == len(node_groups):
                node_groups.append([])

            node_groups[depth].append(node.val)

            tree_traverse(node.left, depth + 1)
            tree_traverse(node.right, depth + 1)
        
        tree_traverse(root, 0)
        return node_groups
```

`if depth == len(node_groups)` のところを少し解答をみた。新たなレベルが追加された場合の判別法を思いつくことができなかった。

node_groups の初めのインデックスで深さを指しているので、len(node_gropus)+1が深さ(1からスタート)と一致する

stackでDFSする

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def levelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:
        if not root:
            return []

        node_groups = []
        node_depth = [(root, 0)]

        while node_depth:
            node, depth = node_depth.pop()
            if depth == len(node_groups):
                node_groups.append([])
            node_groups[depth].append(node.val)

            if node.right:
                node_depth.append((node.right, depth + 1))
            if node.left:
                node_depth.append((node.left, depth + 1))

        return node_groups
```

- https://github.com/h-masder/Arai60/pull/29についてなんで `while` でリストを増やす処理をしているのかわかりやすくまとめてくださっている
    - 同じ処理でも `while` ならその文を読んだだけで、足りるまでリストが追加されていることがわかる
