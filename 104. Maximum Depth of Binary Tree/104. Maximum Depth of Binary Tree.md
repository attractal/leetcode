# Step 1

### BFS

- 上から順に一層ずつ足していく
- 同じ深さでの探索順序は順不同より `list` でも `deque` でもどちらでもよさそう
- 進む向きは一方向で重複無しなので `visited` のような変数は不要
- 空のケースを考えられなかった。入力の確認に注意

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0

        level_node = deque([root])
        depth = 0

        while level_node:

            level_size = len(level_node)
            depth+=1

            for _ in range(level_size):
                node = level_node.popleft()

                if node.right:
                    level_node.append(node.right)
                if node.left:
                    level_node.append(node.left)
        
        return depth
```

### DFS

- 再帰で考える
    - 終了条件： `node` が空
    - 部下にたのむこと： `node.right` と `node.left` のどちらか大きい方の深さ

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0
        return 1 + max(self.maxDepth(root.right), self.maxDepth(root.left))
```

# Step 2

- https://github.com/nittoco/leetcode/pull/14/commits/f88affbf9dceed3e6c60711cff87114715631c3e#r1622609156
    - 下まで到達したら一歩戻って探索
    - このコードの `return` は下から上へ戻るための合図
    - 自分のコードの `return` は戻り値(結果)を報告されるのを上で待つイメージ?
 
```python
  class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0

        level_node = deque([root])
        depth = 0

        while level_node:

            level_size = len(level_node)
            depth+=1

            for _ in range(level_size):
                node = level_node.popleft()

                if node.right:
                    level_node.append(node.right)
                if node.left:
                    level_node.append(node.left)
        
        return depth
```
